name: my-first-action
run-name: ${{ inputs.deploy_target }} by @${{ github.actor }}
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "hello world"
      - name: Print Bye
        run: echo "Bye..."
